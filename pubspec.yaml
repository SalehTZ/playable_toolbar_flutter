name: playable_toolbar_flutter
description: A playable and customizable and easy-to-use toolbar made with flutter
version: 0.0.2
homepage: https://github.com/SalehTZ/playable_toolbar_flutter

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
